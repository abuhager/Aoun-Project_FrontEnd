# Aoun QA02 — API & Security

نفّذ الاختبارات بالترتيب. لا تغيّر الكود ولا تضع أسرارًا في الملفات.

1. شغّل Backend وFrontend محليًا.
2. افتح `QA-02-TEST-CASES.md`.
3. سجّل النتيجة في `QA-02-EXECUTION-RESULTS.md`.
4. عند الفشل استخدم قالب Bug Report.

ابدأ بـ `QA02-API-001` وافتح:

`http://localhost:5000/health/live`

المتوقع HTTP 200 واستجابة صحة. ثم نفّذ `QA02-API-002` على `/health/ready`.
